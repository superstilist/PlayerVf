# Packaged Linux Tools

Files in this directory are copied into the Linux runtime bundle under `tools/`.

For bundled video playback, place mpv at:

```text
linux/packaged/mpv/bin/mpv
```

Use `tool/package_linux_mpv.sh` on Linux to create that folder from a local mpv
install for redistribution with PlayerVF.
